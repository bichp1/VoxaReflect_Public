# VoxaReflect configuration (.env)
# Copy this template, fill in the placeholders, and ensure Backend/app.py loads it (via python-dotenv or `export`).

# API key used by the OpenAI clients. Required: the backend now reads OPENAI_API_KEY directly from the environment.
OPENAI_API_KEY=YOUR_OPENAI_API_KEY

# Reflection assistant model (override to switch between GPT tiers easily).
VOXAREFLECT_LLM_MODEL=gpt-5.1
VOXAREFLECT_CLASSIFIER_MODEL=gpt-5-nano

# Text-to-speech mode: set to "ai" to enable OpenAI TTS, or "none" to disable audio generation.
VOXAREFLECT_TTS_MODE=ai

# OpenAI TTS endpoint; leave as the default unless instructed otherwise.
VOXAREFLECT_TTS_ENDPOINT=https://api.openai.com/v1/audio/speech

# Authorization header; the backend expects a full header value.
VOXAREFLECT_TTS_AUTH_TOKEN="Bearer ${OPENAI_API_KEY}"

# Optional extra headers as JSON (keep empty braces if unused).
VOXAREFLECT_TTS_HEADERS={}

# Return format (mp3/mpeg recommended) and timeout in seconds.
VOXAREFLECT_TTS_FORMAT=audio/mpeg
VOXAREFLECT_TTS_TIMEOUT=15

# Cache TTL (seconds) for synthesized clips in memory.
VOXAREFLECT_TTS_CACHE_TTL=300

# Primary TTS model name.
OPENAI_TTS_MODEL=gpt-4o-mini-tts

# Default fallback voice if no preset-specific voice is resolved.
OPENAI_TTS_DEFAULT_VOICE=cedar

# Style-specific overrides: keep instructions short to bias delivery.
OPENAI_TTS_INSTRUCTION_WARM=Use a warm, encouraging tutoring tone with gentle energy, meant to act as a supportive mentor.
OPENAI_TTS_INSTRUCTION_PROFESSIONAL=Speak calmly and professionally, as an academic mentor.
OPENAI_TTS_VOICE_WARM=cedar
OPENAI_TTS_VOICE_PROFESSIONAL=marin

# Allowed voice ids (comma separated). Leave empty to allow any voice the API supports.
OPENAI_TTS_ALLOWED_VOICES=alloy,cedar,marin,nova

# Frontend client settings:
# Set VOXAREFLECT_FRONTEND_API_BASE to the backend URL that the React app should call when running via npm start.
# Leave it empty to default to the current origin (useful when the frontend is served by Flask/ngrok).
VOXAREFLECT_FRONTEND_API_BASE=http://localhost:5001/

# URL that helper scripts should open after boot; change to your ngrok URL when tunnelling.
VOXAREFLECT_FRONTEND_ENTRY_URL=http://localhost:3000/?lang=de&mic=1&ava=1&group=1
